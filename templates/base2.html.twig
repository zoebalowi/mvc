<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset('img/glider.svg') }}">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        
        <header class="site-header">
            <span class="site-title">My Symfony Site</span>
        </header>
        
        <nav>
            <ul>
                <li><a href="{{ path('pig_start') }}">Pig Game</a></li>

            </ul>
        </nav>

        <main>
            {% block main %}{% endblock %}

        </main>
        <footer>
            <a>denna hemsida är skapad av mig Zoë Waters.</a>
        </footer>
    </body>
</html>
